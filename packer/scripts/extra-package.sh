# yum install -y tree
# yum install --enablerepo=epel -y p7zip
# bash -c "source /etc/profile.d/rvm.sh; rvmsudo gem install debase"
# bash -c "source /etc/profile.d/rvm.sh; rvmsudo gem install ruby-debug-ide"
